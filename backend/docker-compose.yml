services:
  db:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: think9
      POSTGRES_PASSWORD: think9
      POSTGRES_DB: think9
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U think9 -d think9"]
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"

volumes:
  pgdata:
